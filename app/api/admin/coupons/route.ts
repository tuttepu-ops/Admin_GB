import {NextRequest,NextResponse} from 'next/server';
import {requireAdmin} from '../../../../lib/admin-auth';
import {supabaseAdmin} from '../../../../lib/supabase-server';
const clean=(v:any)=>({code:String(v.code||'').trim().toUpperCase(),discount_percent:Number(v.discount_percent||5),minimum_order:Number(v.minimum_order||0),maximum_discount:v.maximum_discount===null||v.maximum_discount===''?null:Number(v.maximum_discount),active:v.active!==false,starts_at:v.starts_at||null,ends_at:v.ends_at||null});
export async function POST(req:NextRequest){try{await requireAdmin(req);const body=clean(await req.json());if(!body.code)return NextResponse.json({error:'Coupon code is required'},{status:400});const db=supabaseAdmin();const {data,error}=await db.from('coupons').insert(body).select('*').single();if(error)throw new Error(error.message);return NextResponse.json(data);}catch(e){const status=(e as Error&{status?:number}).status||500;return NextResponse.json({error:(e as Error).message},{status});}}
export async function PATCH(req:NextRequest){try{await requireAdmin(req);const raw=await req.json();if(!raw.id)return NextResponse.json({error:'Coupon id is required'},{status:400});const db=supabaseAdmin();const {data,error}=await db.from('coupons').update(clean(raw)).eq('id',raw.id).select('*').single();if(error)throw new Error(error.message);return NextResponse.json(data);}catch(e){const status=(e as Error&{status?:number}).status||500;return NextResponse.json({error:(e as Error).message},{status});}}
export async function DELETE(req:NextRequest){try{await requireAdmin(req);const id=new URL(req.url).searchParams.get('id');if(!id)return NextResponse.json({error:'Coupon id required'},{status:400});const db=supabaseAdmin();const {error}=await db.from('coupons').delete().eq('id',id);if(error)throw new Error(error.message);return NextResponse.json({ok:true});}catch(e){const status=(e as Error&{status?:number}).status||500;return NextResponse.json({error:(e as Error).message},{status});}}
